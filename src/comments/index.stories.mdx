import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { props } from '../article-layout/index.stories.mdx';
import Comments from './';

<Meta title="Core/Comments" component={Comments} />

# Comments component

Loads the Coral comments widget.

<Canvas>
  <Story name="Basic" parameters={{ chromatic: { disable: true } }}>
    <Comments flags={props.flags} url="http://localhost:8080" id={props.id} />
  </Story>
</Canvas>
