import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { props } from '../article-layout/index.stories.mdx';
import Header from './';

<Meta title="Core/Header" component={Header} />

# Header component

Basic FT.com header. It takes no props.

<Canvas>
  <Story name="Example">
    <Header flags={props.flags} />
  </Story>
</Canvas>
